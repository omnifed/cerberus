---
title: 'Toggle'
description: 'Toggle allows users to toggle between states via an action.'
npm: '@cerberus-design/react'
source: 'components/toggle'
ark: 'toggle'
---

import {
  WhenNotToUseAdmonition,
  NoteAdmonition,
} from '@/app/components/Admonition'
import CodePreview from '@/app/components/CodePreview'
import BasicPreview from '../components/toggle/static'

The `Toggle` component is a two-state button that allows users to toggle between states via an action.

<WhenNotToUseAdmonition description="If you need to conditionally render content based on a binary condition, use the Show component." />

```ts
import { ToggleParts } from '@cerberus/react'
```

## Usage

The Toggle component is unstyled to fit any scenario you may need.

<CodePreview preview={<BasicPreview />}>
```tsx title="notification-toggle.tsx"
import { NotificationOff, Notification } from '@carbon/icons-react'
import { ToggleParts, IconButton } from '@cerberus/react'

export default function BasicPreview() {
  return (
    <ToggleParts.Root defaultPressed={true} asChild>
      <IconButton ariaLabel="Notification preferences">
        <ToggleParts.Indicator fallback={<NotificationOff />}>
          <Notification />
        </ToggleParts.Indicator>
      </IconButton>
    </ToggleParts.Root>
  )
}
```
</CodePreview>

## Primitives

You can utilize the primitive components or the `css` prop to customize the toggle.

| Component | Description |
| --------- | ----------- |
| ToggleRoot    | The context provider for the Toggle parts |
| ToggleIndicator   | The indicator shown based on the toggle state |

## API

### Parts

The `ToggleParts` API is an Object containing the full family of components.

<NoteAdmonition description="It is best to only use the ToggleParts if you are building a custom solution. Importing Object based components will ship every property it includes into your bundle, regardless if you use it or not." />

| Name     | Description                            |
| -------- | -------------------------------------- |
| Root     | The `ToggleRoot` component which is the Provider for the family. |
| Indicator  | The `ToggleIndicator` component which displays based on the pressed state. |
