---
title: Filtering
description: Learn about the filtering feature in the Data Grid component.
package: 'data-grid'
---

import { NoteAdmonition } from '@/app/components/Admonition'
import CodePreview from '@/app/components/CodePreview'
import { DEMOS } from '../components/filtering/static'

## Overview

Filtering allows users to view a subset of data based on criteria. There are
three ways for a user to filter:

1. **Single-column filtering**: Clicking the "Filter" option from the feature menu when the column has opted into filtering.
2. **Multi-column filtering**: Clicking the plus icon to add a new row to the single-column filter option from #1.
3. **Global filtering**: Utilizing the `setGlobalFilter` action from the [Data Grid Context](/docs/data-grid/context).

**Active Actions**

Once a filter is active, the user can:

1. **Edit filters**: Clicking the added "Edit filter" icon button that displays in the column header that has the active filter.
2. **Clear filters**: Clicking the "Clear Filter" option in the column feature menu. This will reset the state completely.

<CodePreview {...DEMOS.basic} />

## Implementing column filtering

You must opt-in to column filtering by setting the `features.filter` prop to `true` or passing in a custom filter function. Doing so will display the filter option in the column menu.

<CodePreview {...DEMOS.basic} />

## Multi-column Filtering

Multi-column filtering is enabled by default. The user simply adds a new column-based filter group by pressing the "plus" icon in the popover.

<CodePreview {...DEMOS.basic} />

## Custom Comparator

A comparator determines how two cell values should be sorted. The default comparator
uses the vanilla JavaScript basic comparison of `a` and `b`.

When using the `accessorFn` method, the comparator uses the value returned from the
`accessorFn` callback provided as the first argument.

To provide a custom comparator, you can pass a function as the `features.sort.comparator` option.

<NoteAdmonition description="Dates will naturally sort in order without any customization. This example just shows how to customize the sorting behavior." />

<CodePreview {...DEMOS.basic} />

## Options

The `sort` feature accepts either a `boolean` or an object with the following properties:

<CodePreview {...DEMOS.data} />

| Params               | Required | Description                                |
| -------------------- | -------- | ------------------------------------------ |
| `firstSortDirection` | `false`  | The initial sort direction for the column. |
| `comparator`         | `false`  | A custom comparator function for sorting.  |
