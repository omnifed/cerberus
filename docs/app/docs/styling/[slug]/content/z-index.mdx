---
title: z-index
description: The z-index property controls the vertical stacking order of elements that overlap.
---

import ZList from '../components/z-index/z-list'
import ZPreview from '../components/z-index/z-preview'

## Overview

Use the `zIndex` to apply layering to an element.

```tsx
<Box zIndex="sticky" />
```

| Prop                   | CSS Property     | Token Category |
| ---------------------- | ---------------- | -------------- |
| `zIndex`               | `z-index`        | `zIndex `      |

## Layer Playground

<ZPreview />

## Values

<ZList />