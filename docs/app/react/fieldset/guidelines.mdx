---
---

import CodePreview from '@/app/components/CodePreview'
import OverviewList from '@/app/components/OverviewList'
import DripDivider from '@/app/components/drip-divider'
import { BasicDemo, FieldsetGroupDemo } from '@/app/react/fieldset/components/static-preview'

## Usage

## Form Groups

Form groups are used to group related form elements together within a form

<CodePreview preview={<BasicDemo />} />

<DripDivider />

## Field Groups

Field groups are used to group related form elements together outside of a form - mostly used fro Radio and Checkbox groups.

<CodePreview preview={<FieldsetGroupDemo />} />