---
---

import CodePreview from '@/app/components/CodePreview'
import OverviewList from '@/app/components/OverviewList'
import {
  WhenToUseAdmonition,
  WhenNotToUseAdmonition,
} from '@/app/components/Admonition'
import {
  BasicTablePreview
} from '@/app/react/table/components/table-preview'

## Usage

A table provides a way to organize data in a grid format. Tables are used to display data-centric information and can contain actions, such as buttons or links.

<WhenToUseAdmonition description="When you need to display multiple rows of information that cannot be done in a list format." />

<WhenNotToUseAdmonition description="If you can display information in a list format which allows for a better user experience." />


## Standard Table

<CodePreview preview={<BasicTablePreview />} />
