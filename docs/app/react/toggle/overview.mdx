---
heading: 'Toggle'
description: 'Toggles switch the selection of an item on or off'
a11y: 'forms'
---

import CodePreview from '@/app/components/CodePreview'
import OverviewList from '@/app/components/OverviewList'
import {
  OverviewToggleGroup,
  OverviewToggleSizes
} from '@/app/react/toggle/components/toggle-preview'

<OverviewList rules={[
  'Use toggles (not radio buttons) if the items in a list can be independently controlled',
  "Toggles are the best way to let users adjust settings",
  "Make sure the toggle's selection (on or off) is visible at a glance"
]} />

## Example

<CodePreview preview={<OverviewToggleGroup />} />
<CodePreview preview={<OverviewToggleSizes />} />

## Resources

| Name     | Resource | Status                                               |
| -------- | -------- | ---------------------------------------------------- |
| Figma    | [Design Kit (Figma)](https://www.figma.com/design/ducwqOCxoxcWc3ReV3FYd8/Digital-University-Component-Library?m=auto&node-id=0-1)      |                         Private                      |