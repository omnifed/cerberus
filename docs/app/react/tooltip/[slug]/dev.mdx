---
npm: '@cerberus-design/react'
source: 'components/Tooltip.tsx'
recipe: 'slots/tooltip.ts'
---

import CodePreview from '@/app/components/CodePreview'
import {
  NoteAdmonition
} from '@/app/components/Admonition'
import {
  BasicTooltipPreview,
  PositioningTooltipPreview,
} from '@/app/react/tooltip/components/tooltip-preview'
import {
  LivePlaygroundWithCode,
} from '@/app/react/tooltip/components/live-playground'

```ts
import { Tooltip } from '@cerberus/react'
```

## Usage

<LivePlaygroundWithCode />

## Customization

To customize the tooltip, we recommend extending the Tooltip slot recipe found in the preset.

## API

```tsx
export interface TooltipProps {
  content: ReactNode
  position?: 'top' | 'right' | 'bottom' | 'left'
}
```

| Name     | Default | Description                                                   |
| -------- | ------- | ------------------------------------------------------------- |
| content  |   | The content to display in the tooltip      |
| position | 'top' | The position of the tooltip relative to the target element |
