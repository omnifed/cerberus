---
---

import CodePreview from '@/app/components/CodePreview'
import OverviewList from '@/app/components/OverviewList'
import {
  WhenToUseAdmonition,
  WhenNotToUseAdmonition,
} from '@/app/components/Admonition'
import {
  BasicTooltipPreview
} from '@/app/react/tooltip/components/tooltip-preview'

## Usage

A tooltip provides additional context for a UI element.

Tooltips briefly describe a UI element. They're best used for labelling UI elements with no text, like icon-only buttons and fields.

<WhenToUseAdmonition description="When you need to describe what a visual element is within the UI." />

<WhenNotToUseAdmonition description="When you need to provide a tooltip that contains actions - use the Popover." />


## Standard Tooltip

<CodePreview preview={<BasicTooltipPreview />} />

<WhenToUseAdmonition description="When you need to describe a UI element." />

## Placement

By default, plain tooltips are positioned directly above the parent element.

If the element is in a top app bar, the plain tooltip appears below the element at the same distances.
