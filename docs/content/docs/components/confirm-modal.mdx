---
title: Confirm Modal
description: 'The Confirm Modal component provides a way to confirm actions with users, ensuring they are aware of the consequences before proceeding.'
npm: '@cerberus-design/react'
source: 'context/confirm-modal.tsx'
recipe: 'slots/dialog.ts'
ark: 'dialog'
---

import CodePreview from '@/app/components/CodePreview'
import { CodeSnippet } from '@/app/components/code-snippet'
import { NoteAdmonition } from '@/app/components/Admonition'
import { DEMOS } from '../components/confirm-modal/static'

<CodeSnippet snippet={DEMOS.meta} />

## Usage

The `ConfirmModal` component is a controlled abstraction of the [Dialog](/docs/components/dialog).

<CodePreview {...DEMOS.basic} />

## Destructive

To show a destructive confirm modal, set the `kind` prop in the options to `'destructive'`.

<CodePreview {...DEMOS.destructive} />

### No Avatar

To change the visibility of the avatar, set `showAvatar` to `false` in the options.

<CodePreview {...DEMOS.avatar} />

## Primitives

The layers of the `ConfirmModal` which can be used to create a fully custom solution are the same as the [Dialog](/docs/components/dialog#primitives).

### Data Attributes

The primitives additionally use the same data attributes as the [Dialog](/docs/components/dialog#primitives).

## API

### ConfirmModal

The `ConfirmModal` component is an abstraction of the Dialog and accepts the same
props as the [`Dialog`](/docs/components/dialog#api) component.

### Show Method Options

The `show` method accepts the following options:

| Name          | Default           | Description                                                                        |
| ------------- | ----------------- | ---------------------------------------------------------------------------------- |
| `showAvatar`  | `true`            | Whether to show the avatar in the modal.                                           |
| `kind`        | `non-destructive` | The variant used to theme the modal.                                               |
| `heading`     |                   | The heading of the modal.                                                          |
| `description` |                   | The description of the modal. For non-destructive modals only, can be a ReactNode. |
| `actionText`  |                   | The text for the action button.                                                    |
| `cancelText`  |                   | The text for the cancel button.                                                    |
